import { Template } from "@src/components/layout";
import { MainContent } from "./components/mainContent";

const Home = () => {
  return (
    <Template>
      <MainContent />
    </Template>
  );
};

export default Home;
